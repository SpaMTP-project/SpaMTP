% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeneMapping.R
\name{annotateGeneIdentifiers}
\alias{annotateGeneIdentifiers}
\title{Store gene mapping in a native experiment's row metadata}
\usage{
annotateGeneIdentifiers(
  object,
  assay = "main",
  id_column = NULL,
  id_type = c("auto", "symbol", "entrez", "ensembl", "uniprot", "hgnc"),
  ...
)
}
\arguments{
\item{object}{A SummarizedExperiment, SingleCellExperiment or
SpatialExperiment, including native objects provided by SpaMTPData.}

\item{assay}{The primary experiment (\code{"main"}) or transcriptome altExp name.}

\item{id_column}{Gene identifier column in the selected \code{rowData()}.
\code{NULL} uses its row names.}

\item{id_type}{Identifier type. \code{"auto"} accepts the same input formats as
\code{\link[=mapGeneIdentifiers]{mapGeneIdentifiers()}}. Other values explicitly prefix unqualified IDs.}

\item{...}{Passed to \code{\link[=mapGeneIdentifiers]{mapGeneIdentifiers()}}, including \code{organism}, \code{index},
\code{database} and versioned reference settings.}
}
\value{
The input object with \verb{spamtp_gene_*} mapping columns and
\code{metadata(experiment)$gene_mapping} provenance and input audit.
}
\description{
Adds aligned mapping columns to the selected experiment's \code{rowData()} and
provenance to its \code{metadata()}. Expression values, feature names, row order,
spatial coordinates and paired experiments are preserved. Multiple assay
rows that map to one gene are annotated, not silently summed; pathway
enrichment performs gene-level deduplication separately.
}
\examples{
utils::str(formals(annotateGeneIdentifiers))
}
