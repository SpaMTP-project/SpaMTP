% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeneMapping.R
\name{mapGeneIdentifiers}
\alias{mapGeneIdentifiers}
\title{Map gene identifiers with an auditable HGNC identity}
\usage{
mapGeneIdentifiers(
  genes,
  database = NULL,
  index = NULL,
  gene_reference = NULL,
  gene_reference_version = "latest",
  gene_reference_local_dir = NULL,
  organism = "Homo sapiens",
  ambiguous = c("exclude", "error"),
  verbose = TRUE,
  database_version = "latest",
  database_local_dir = NULL,
  offline = FALSE
)
}
\arguments{
\item{genes}{Vector of gene identifiers.}

\item{database}{Optional named SpaMTPdb resource bundle with \code{source_df}.}

\item{index}{Optional reusable index from \code{\link[=buildGeneMappingIndex]{buildGeneMappingIndex()}}. If
provided, its recorded source and reference determine the mapping. If
\code{database} is also supplied, its source-table fingerprint must match.}

\item{gene_reference}{HGNC reference data frame. \code{NULL} retrieves the pinned
reference through \code{\link[SpaMTPdb:spaMTPdbGeneReference]{SpaMTPdb::spaMTPdbGeneReference()}}. Custom references
require unique \code{hgnc_id} and \code{symbol}; optional fields are \code{status},
\code{entrez_id}, \code{ensembl_gene_id}, \code{uniprot_ids}, \code{prev_symbol}, \code{alias_symbol}.}

\item{gene_reference_version}{Version in SpaMTPdb's separate HGNC registry.}

\item{gene_reference_local_dir}{Local HGNC resource directory.}

\item{organism}{Must be \code{"Homo sapiens"}; this index is not a mouse-to-human
orthology map. Use a separately curated species-specific database for other
organisms.}

\item{ambiguous}{Exclude ambiguous mappings with audit status, or error.}

\item{verbose}{Warn about unresolved input rows.}

\item{database_version}{RaMP version, independent of the HGNC version.}

\item{database_local_dir}{Local staged RaMP directory.}

\item{offline}{Require local or cached HGNC data.}
}
\value{
One row per input with \code{gene_id} (HGNC), approved \code{symbol},
\code{canonical_ramp_id}, all equivalent \code{ramp_ids} (semicolon separated),
\code{status}, mapping \code{method} and \code{candidate_gene_ids}. The \code{gene_mapping}
attribute records provenance. \code{mapped_ramp_only} explicitly denotes an
unharmonized RaMP record without a resolved HGNC identity.
\code{candidate_ramp_ids} records raw candidates encountered for direct RaMP
IDs or source crossreferences, including unresolved/conflicting records.
A shared crossreference with unresolved competitors remains \code{ambiguous}
even if only one candidate has a resolved HGNC identity.
}
\description{
Accepts approved, previous and alias symbols (bare or \verb{gene_symbol:}), HGNC,
Entrez, Ensembl gene IDs (optional version suffix), UniProt and RaMP IDs.
Ambiguous identifiers are excluded, never expanded to multiple genes.
Different RaMP records of the same resolved HGNC gene share one canonical
RaMP ID. Gene references are supplied by SpaMTPdb; this function does not
query live annotation services. Input order and duplicates are preserved.
}
\examples{
utils::str(formals(mapGeneIdentifiers))
}
