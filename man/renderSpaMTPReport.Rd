% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WorkflowReport.R
\name{renderSpaMTPReport}
\alias{renderSpaMTPReport}
\title{Render a portable, self-contained multi-omics report}
\usage{
renderSpaMTPReport(
  result,
  output_dir,
  max_points = 5000,
  interactive = TRUE,
  preview_features = 180,
  preview_points = min(max_points, 2000),
  table_rows = 1000
)
}
\arguments{
\item{result}{A spamtp_workflow result from runSpaMTPWorkflow.}

\item{output_dir}{New or empty output directory.}

\item{max_points}{Maximum observations displayed per scatter plot. Analyses
and exported tables are not subsampled.}

\item{interactive}{Embed an offline region/DE explorer using local JavaScript.}

\item{preview_features}{Maximum feature profiles embedded in the explorer.}

\item{preview_points}{Maximum spatial/distribution observations embedded.}

\item{table_rows}{Maximum preloaded ranked rows per region or contrast.
Full tables are exported independently of this preview limit.}
}
\value{
Absolute path to report.html, invisibly.
}
\description{
The HTML embeds PNG figures and styles and needs no network, Pandoc or R
session to view. Full tables, mapping weights, processing settings and the
analysed object are also saved for reproducibility. Existing files are never
overwritten. A report is published to output_dir only after rendering succeeds.
}
\examples{
utils::str(formals(renderSpaMTPReport))
}
