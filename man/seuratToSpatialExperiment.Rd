% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ConvertingBetweenObjects.R
\name{seuratToSpatialExperiment}
\alias{seuratToSpatialExperiment}
\title{Convert a Seurat object to SpatialExperiment}
\usage{
seuratToSpatialExperiment(
  x,
  assay = NULL,
  layer = "counts",
  image = NULL,
  includeAltExps = TRUE,
  coordinates = NULL
)
}
\arguments{
\item{x}{A Seurat object.}

\item{assay}{Primary Seurat assay; defaults to its active assay.}

\item{layer}{Exact layer to convert, usually \code{"counts"}.}

\item{image}{Optional Seurat image/FOV used for coordinates. A single image
is selected automatically; its coordinates take precedence over metadata.}

\item{includeAltExps}{Convert other assays with matching pixels.}

\item{coordinates}{Optional matrix/data.frame with x/y columns and pixel
names in row names or a \code{cell} column. Cannot be combined with image.}
}
\value{
A SpatialExperiment.
}
\description{
Convert once at the workflow boundary, then use Bioconductor containers
throughout SpaMTP. Expression and metadata follow
\code{\link[=seuratToSingleCellExperiment]{seuratToSingleCellExperiment()}}. Coordinates are matched by pixel names,
never by position. Supply \code{coordinates} for multi-image data or image
formats without x/y columns; coordinate units and orientation must already
be consistent. Optical rasters, graphs and reductions are not transferred.
Attach optical images separately with \code{\link[=addSpatialImage]{addSpatialImage()}}.
When a single image/FOV is present, its public \code{GetTissueCoordinates()}
accessor is the default coordinate source, even if historical x/y columns
remain in cell metadata. Metadata coordinates are used automatically only
when there are no images. Multiple images always require an explicit
selection. Centroids are read as pixel centres, not expanded polygon
vertices. The selected source is recorded in
\code{metadata(x)$seurat_interoperability}; retained legacy coordinate columns
are archival and must not replace \code{spatialCoords(x)} in native analysis.
}
\examples{
if (requireNamespace("SeuratObject", quietly = TRUE)) {
    counts <- matrix(seq_len(12), nrow = 3,
        dimnames = list(paste0("mz-", 101:103), paste0("pixel", 1:4)))
    seu <- SeuratObject::CreateSeuratObject(counts)
    xy <- cbind(x = 1:4, y = c(0, 1, 0, 1))
    rownames(xy) <- colnames(seu)
    spe <- seuratToSpatialExperiment(seu, coordinates = xy)
    spe <- normalizeSMData(spe, verbose = FALSE)
}
}
